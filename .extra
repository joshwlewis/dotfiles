# make sure g alias works for git-completion
complete -o default -o nospace -F _git g

# Git credentials
git config --global user.name "Josh W Lewis"
git config --global user.email "josh.w.lewis@gmail.com"

