# Editor
abbr -a vi nvim
abbr -a vim nvim

# Heroku
abbr -a h heroku
abbr -a hs heroku sudo

# Git
abbr -a g git

# ls (using lsd)
abbr -a ls lsd
abbr -a ll lsd -lF
abbr -a la lsd -aF
abbr -a lla lsd -laF
abbr -a lt lsd --tree

# map
abbr -a map xargs -n1
